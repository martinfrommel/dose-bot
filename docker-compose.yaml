services:
  dosebot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dosebot
    ports:
      - "8910:8910"  # Web
      - "8911:8911"  # API
    volumes:
      # Persist SQLite database
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/dev.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8910"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
